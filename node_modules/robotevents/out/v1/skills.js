"use strict";
/**
 * Support programmatic access to the Skills Leaderboard using the v1 API
 */
Object.defineProperty(exports, "__esModule", { value: true });
const request_1 = require("../util/request");
/**
 *
 * @param season Season ID
 * @param options Search Options
 * @param maxAge Maximum allowable age when using a cached value. If not
 * specified, any suitable record from the cache may be used
 */
async function getSkillsLeaderboard(season, options, maxAge) {
    let results = await (0, request_1.requestSingle)(`/api/seasons/${season}/skills`, { grade_level: options.grade_level, post_season: options.post_season }, maxAge);
    if (options.region) {
        results = results.filter((spot) => spot.team.eventRegion === options.region);
    }
    return results;
}
exports.default = getSkillsLeaderboard;
//# sourceMappingURL=skills.js.map